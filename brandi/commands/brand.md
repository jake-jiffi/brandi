---
description: Build or continue a complete brand and design system, with visual rounds on the /design canvas
argument-hint: [brand name, a URL, a description, or nothing at all]
---

Run the brand-system journey. Invoke the `brand-system` skill and follow it exactly.

What the user said, which may be empty: $ARGUMENTS

Start by running `brandi status`. If a brand file already
exists, resume from the phase it reports rather than starting over. If there is none, run `init` and
begin at Recon.

Two things the user cares about, so get them right:

- Ask once, in a single batched question, and never again. Everything else you work out yourself or
  file as an open question with a stated working assumption.
- The visual rounds are the point. Territories and Proof both end with a published canvas the user
  can look at, not a description of one.
